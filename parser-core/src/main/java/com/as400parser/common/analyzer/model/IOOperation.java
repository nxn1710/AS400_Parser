package com.as400parser.common.analyzer.model;

/**
 * Enumerates all potential Input/Output operations that logic modules execute against a file.
 */
public enum IOOperation {
    READ,
    READE,
    READP,
    READPE,
    WRITE,
    UPDATE,
    DELETE,
    CHAIN,
    SETLL,
    SETGT,
    EXFMT,
    OPEN,
    CLOSE
}
